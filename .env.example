# Database Configuration
# You can specify full DATABASE_URL or separate connection parameters

# Full database connection URL (priority option)
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/financial_tracker

# Or use separate connection parameters:
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=financial_tracker
